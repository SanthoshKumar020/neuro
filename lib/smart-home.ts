export async function controlDevice(device: string, action: string) {
  // Placeholder: integrate IFTTT / Google Home / Puppeteer
  console.log(`[Smart Home] ${device} -> ${action}`);
  // Add device logic in future phase
}